import { ComponentController } from '../../modules/Components/Component.js';

export class PersonalInfoBoxController extends ComponentController {
  constructor(model, view, component) {
    super(model, view, component);
  }
}
